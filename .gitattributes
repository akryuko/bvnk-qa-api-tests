# Normalise line endings: LF in the repository, native in the working copy.
# Without this, committing from Windows records CRLF and every file looks
# modified to anyone on macOS or Linux.
* text=auto eol=lf
